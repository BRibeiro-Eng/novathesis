%!TEX root = ../template.tex
%% ====================================================================
%% Capitulo 4 -- Materiais e metodos
%% Primeira versao completa 2026-09-08. Linguagem descritiva, sem rotulos
%% internos de organizacao do trabalho.
%% Fontes da vistoria (2026-09-08): ~/tese-extraccao (extraccao integral do
%% modelo Power BI v22 de 2026-07-31: modelo.bim, 159 medidas DAX, 21 queries M,
%% 32 particoes, metadados de reflexao, 25 TSV exportados, inventario e
%% manifesto MD5 das fontes, 33 prints), notas do Mind Palace (mapeamento e
%% validacao de jul-2026, memoria do projeto), PROTOCOL.md / CLAUDE.md e
%% docs/ do repositorio baselines-cc.
%% Marcas %% CONFIRMAR assinalam factos a confirmar pelo autor.
%% ====================================================================

\chapter{Materiais e métodos}
\label{cha:metodos}
%% 2026-09-21: fecha a caixa C4-01. Os inventarios de extracao e de
%% modelo, as contagens de testes e a mecanica de publicacao de corridas
%% foram encolhidos e remetidos para o Apendice D; ficaram no corpo os
%% erros que alteram os dados (vapor sub-reportado, electricidade
%% duplicada), porque esses mudam a validade dos resultados. Na mesma
%% passagem, o vocabulario de codigo saiu do corpo: nomes de tabelas e de
%% ficheiros, ``gate'', ``write-once'', ``locf'', ``backfill'',
%% ``fingerprint'', ``snapshot'' e ``slate''.

Este capítulo descreve o que foi construído e como foi avaliado. As três
primeiras secções tratam da plataforma: de onde vêm os dados, como foram
reconciliados num modelo dimensional único e que métricas se calculam sobre
ele. As duas últimas tratam do método de avaliação das \emph{baselines} que
os Capítulos~\ref{cha:diagnostico} e~\ref{cha:discussao} aplicam --- o
protocolo estatístico e a infraestrutura de reprodutibilidade que garante
que cada número citado nesta dissertação pode ser refeito. A matemática de
cada instrumento consta do Apêndice~\ref{app:matematica}; aqui descreve-se o
desenho e as decisões que o justificam.

\section{Fontes de dados e ingestão}
\label{sec:fontes}

O consumo energético de uma unidade processual não existe numa fonte
única. Na refinaria de Sines, a informação necessária para o calcular está
repartida por três famílias de origens, com resoluções, unidades e
responsáveis diferentes, e a primeira decisão de desenho da plataforma foi
não escolher uma delas: lê-las todas e reconciliá-las por regras
declaradas.

\paragraph{O historiador de processo.} O historiador IP21 regista as
medições de campo --- caudais de combustível e de vapor, carga das
unidades, densidades, temperaturas e pressões de \emph{flash}, poderes
caloríficos --- com resolução horária. \ajuste{Cada ponto de medição é
identificado por uma etiqueta, a \emph{tag}, que é como o historiador e a
refinaria lhe chamam e como este capítulo lhe chama a partir daqui.} O
acesso faz-se através da plataforma de dados da refinaria (Dremio), numa
vista horária por \emph{tag}. A plataforma não consulta o historiador
\emph{tag} a \emph{tag}: a partir da tabela de mapeamento
(Secção~\ref{sec:modelo-dimensional}), agrega cada \emph{tag} ao dia com a
regra que lhe cabe --- soma diária para caudais, média diária para poderes
caloríficos e propriedades, média multiplicada por 24 para caudais
horários, máximo para indicadores --- e devolve uma linha por \emph{tag} e
por dia. \ajuste{A forma de escrever essa consulta teve de ser revista por
razões de desempenho da plataforma de dados, sem consequência para os
valores obtidos.}

\paragraph{Do valor horário ao valor diário.} A vista do historiador
entrega, por \emph{tag} e por hora, o valor médio dessa hora, já com o ano, o
mês e o dia atribuídos; o dia da plataforma é esse dia, e o fuso horário e
o tratamento das mudanças de hora legal são os da vista, que não foram
verificados. Para um caudal em t/h, a massa diária é a soma das médias
horárias, que coincide com a integração da
Equação~\eqref{eq:mat-integracao} quando as 24 horas existem. Não há número
mínimo de horas válidas: uma hora em falta não entra na soma e o dia fica
subcontado sem marca, e um dia sem nenhum valor passa a zero no canal do historiador. Nos combustíveis com poder calorífico medido, a energia diária
é a massa diária multiplicada pela média simples do poder calorífico nesse
dia, e não a soma dos produtos horários, apesar de a regra se chamar assim
na tabela de parâmetros. \revisto{A diferença entre os dois cálculos é proporcional à covariância intradiária entre caudal e poder calorífico}
(Equação~\eqref{eq:mat-covariancia-agregacao}); os dados horários não fazem
parte da extração congelada e a sua dimensão ficou por quantificar. Na
destilação atmosférica os quatro vetores diários avaliados no
Capítulo~\ref{cha:diagnostico} vêm do canal de balanço em todos os dias de
2020 a 2025, pelo que estas regras afetam a validação entre canais e não a
série avaliada.

\paragraph{Os balanços mensais.} O Processamento de Dados da refinaria
mantém dois livros de balanço mensal, com valores diários por referência
de medidor: o balanço de produção (BAL\_PROD; folhas de medidores e de
caudais, em toneladas por mês na coluna mensal e por dia nas colunas
diárias) e o balanço de utilidades (BAL\_UTIL; medidores em toneladas por
hora, vapor por nível de pressão). São a fonte oficial do fecho mensal e a
referência contra a qual os relatórios de consumo são emitidos. A leitura
é feita por funções de ingestão que percorrem as pastas de rede por ano e
mês e extraem, por referência, a série diária. Os dois livros não têm a
mesma estrutura: no de produção o primeiro dia do mês está seis colunas à
direita do identificador, no de utilidades quatro --- e a primeira versão
da ingestão, que assumia o desvio do primeiro em ambos, perdia dois dias
por mês no vapor e somava zeros nos dois últimos, sub-reportando os três
níveis de vapor em 5 a 7\%. O erro foi encontrado na validação da
Secção~\ref{sec:validacao-dados} e é o exemplo do que essa validação existe
para apanhar. O balanço de produção fornece ainda a folha de poderes
caloríficos diários dos combustíveis sem \emph{tag} própria (\emph{off-gas},
\emph{tail gas}, gás natural, coque), lida com uma guarda de gama física
(30 a 30\,000~kcal/kg, porque uma etiqueta repetida na folha produzia, sem
guarda, um poder calorífico médio de dois milhões) e com propagação do
último valor conhecido quando um mês não o traz\ajuste{. Cada valor leva
registada a sua origem --- lido do balanço, propagado do último valor
conhecido ou preenchido a partir do primeiro valor posterior --- para que
medido e propagado nunca fiquem indistinguíveis}.

\paragraph{A energia elétrica.} A eletricidade não tem série diária: existe
nos relatórios mensais de envio de cada fábrica, em MWh por unidade, com
blocos de consumo e de produção. A ingestão lê esses relatórios com um
filtro de nomes canónicos (padrão da fábrica seguido de espaço, sufixo de
envio, e uma guarda anti-duplicado por ano, mês e padrão que fica com o
ficheiro mais recente), porque um ficheiro de segurança deixado na pasta
de leitura --- uma versão anterior à revisão de janeiro de 2026 --- foi
somado ao definitivo durante várias atualizações, duplicando o consumo de
quatro unidades. Desde julho de 2026 a ingestão expõe o consumo bruto ao
lado do líquido (consumo menos produção): o líquido serve o reporte, o
bruto serve a \emph{baseline} e o índice de intensidade, porque uma unidade
que produz o que consome não deve aparecer com consumo nulo numa relação
com a carga.

\paragraph{Planos de fabrico, densidades e crudes.} Os planos de fabrico
semanais (folhas Excel no arquivo de planeamento, 2020 em diante)
fornecem \ajuste{a composição prevista da carga de crudes} por período e
por unidade; as
densidades de rede e as propriedades da carga (densidade, temperatura e
pressão de \emph{flash}, rendimento em resíduo) vêm do historiador e das
folhas de densidades, com um valor por omissão declarado no mapeamento
quando a \emph{tag} não existe --- e uma coluna de origem que diz, dia a
dia, se o valor foi medido ou assumido.

\paragraph{Inventário e extração.} \ajuste{Para que a plataforma possa ser
reconstruída fora da rede da refinaria, e para que os resultados desta
dissertação assentem num estado fixo e não numa ligação viva, o conteúdo
das fontes foi copiado e congelado a 31 de julho de 2026. Copiaram-se os
3\,862 ficheiros que o modelo consome, com cobertura completa dos balanços
mensais de novembro de 2019 a julho de 2026. Cada ficheiro levou registada
uma assinatura do seu conteúdo, e uma verificação posterior confirmou que
só seis tinham mudado na origem entre a cópia e a verificação, todos em
planos de fabrico, que não entram no cálculo da energia, da carga nem das
\emph{baselines}. O modelo do \emph{dashboard} foi igualmente exportado por
inteiro na mesma data, com as suas 25 tabelas em texto; o inventário
detalhado das fontes e da exportação está no Apêndice~\ref{app:manual}.} É
sobre essa exportação --- e não sobre uma ligação viva --- que o
Capítulo~\ref{cha:diagnostico} trabalha.

\section{Modelo dimensional e reconciliação}
\label{sec:modelo-dimensional}
A Figura~\ref{fig:arquitetura-dados} mostra o percurso de um valor desde a origem até ao facto e o esquema em estrela que dele resulta.

\input{5-Figures/tikz/f06-arquitetura-dados}

A plataforma é um modelo tabular em esquema em
estrela~\cite{KimballRoss2013}, \ajuste{com a totalidade dos dados carregada
em memória e não consultada na origem a cada utilização}. Três tabelas de
factos concentram a informação: \ajuste{a energia, com 200\,924 linhas, uma
por dia, unidade e vetor, de 2020-01-01 a 2026-07-31, para 16 unidades e
seis vetores mais o agregado global; a carga, com 45\,408 linhas desde novembro de 2019, com a carga
total e os seus componentes por nível de agregação; e as propriedades da
carga, com 19\,105 linhas em 8 unidades}, a que se juntam densidades, planos
de fabrico e crudes. As dimensões são o calendário (gerado, até ao dia
anterior à atualização), as unidades (16, em três fábricas mais os
extras, com o limiar de carga mínima de cada uma e a sua composição em
secções), os vetores \ajuste{--- seis: fuel gás, vapor a 24, 10 e 3~bar, energia
elétrica, e o coque com os outros combustíveis ---, mais o agregado global}, os crudes
(56) e duas dimensões de apresentação --- janela temporal e métrica ---
que alimentam os seletores do \emph{dashboard}. Os parâmetros físicos que
qualquer cálculo usa vivem em duas tabelas de parâmetros e nunca no
código: o poder calorífico de referência do gás natural
(11\,820~kcal/kg), as entalpias equivalentes dos três níveis de vapor
(698, 738 e 758~kcal/kg), a conversão da eletricidade (860,4~Mcal por MWh e um
factor de eficiência de conversão de 0,57), o número mínimo de
observações para estimar uma \emph{baseline} (30) e, por unidade, o limiar
de carga abaixo do qual um dia não é operacional --- 18\,600~t/d na
destilação atmosférica.

\paragraph{A tabela de mapeamento é o contrato.} O objeto central do
modelo não é um facto, é o mapeamento mestre: 286 linhas, cada uma a
ligar uma unidade física, um vetor e um papel (consumo ou produção) a
uma origem --- uma \emph{tag} do historiador, uma referência de balanço
ou as duas --- com o factor de unidade do canal de \emph{tags}, um factor
de inclusão do canal de balanço (1, 0, $-1$ ou uma fração de alocação),
a \emph{tag} de poder calorífico quando aplicável, datas de vigência e um
comentário de qualidade. Das 286 linhas, 235 apontam para o balanço de
utilidades e 51 para o de produção; 137 têm também \emph{tag} do
historiador; 213 são consumos e 73 produções. Cinco mapeamentos auxiliares
(poderes caloríficos, eletricidade, carga por \emph{tags} e por balanço,
densidades, propriedades) seguem o mesmo princípio: nenhuma constante de
conversão, nenhuma referência de medidor e nenhuma regra de alocação
existe fora de uma tabela versionada. Foi esta separação que permitiu
corrigir, em julho de 2026, a alocação da produção de vapor entre as duas
unidades de vácuo (24\% e 76\% a 24~bar; 69\% e 31\% a 3~bar, fórmulas
oficiais do controlo processual) alterando uma coluna e nenhuma linha de
código.

\paragraph{O que aqui se chama reconciliação.} Nesta dissertação,
reconciliar é harmonizar fontes: pôr os canais lado a lado, aplicar uma
regra de precedência declarada e confrontar os resultados com o relatório
oficial. Não é reconciliação de dados no sentido estatístico, em que as
medições são ajustadas para satisfazer balanços de massa e energia.
\alterado{Esse ajustamento existe, mas a montante deste trabalho: o
canal de balanço é ele próprio o resultado de um procedimento que
redistribui os desajustes por certas correntes até as contas fecharem,
cujas regras não foram auditadas aqui. O que se harmoniza nesta secção
são canais já constituídos, um dos quais é um produto reconciliado e não
uma leitura.} A concordância entre canais não é prova de exatidão
metrológica: dois canais
que leem o mesmo medidor, ou o mesmo balanço, concordam mesmo que o medidor
esteja errado. A calibração dos medidores a montante não fez parte do
trabalho.

\paragraph{Três canais, uma regra de precedência.} A energia de cada
dia, unidade e vetor chega por até três canais --- \ajuste{o historiador},
balanço de produção ou utilidades, e relatórios de eletricidade --- e a tabela unificada guarda os três lado a lado antes de
escolher. A regra é declarada e por linha: balanço, depois eletricidade,
depois \emph{tags}; nunca se mistura um canal com outro na mesma linha, e
uma coluna de origem regista qual venceu. Na carga a regra é a mesma
(balanço antes de historiador) e os componentes da carga seguem a fonte
vencedora. Nos 200\,924 registos de energia, 121\,828 têm balanço e
\ajuste{historiador} em simultâneo --- é a redundância que a validação da
Secção~\ref{sec:validacao-dados} explora ---, 42\,132 só balanço e 36\,964
vêm dos relatórios de eletricidade. Uma quarta coluna, o consumo ``sem
produção'', é igual ao unificado em todos os vetores exceto na
eletricidade, onde usa o bruto: é a coluna que a \emph{baseline} e o índice
de intensidade consomem. Os relatórios operacionais e o reporte de
emissões continuam no líquido.

A confrontação célula a célula da Secção~\ref{sec:validacao-dados} cobre
janeiro a maio de 2026 e não valida, por si, os seis anos anteriores. Nos
dias operacionais da destilação atmosférica de 2020 a 2025, os dois canais
existem em todos os dias dos quatro vetores diários, e a diferença entre
eles muda de ano para ano: no fuel gás, a mediana da diferença relativa do
canal do historiador face ao balanço vai de $-11\%$ em 2020 a $0\%$ em 2023;
nos vapores chega a $\pm30\%$ em alguns anos, e no de 10~bar, que é um fluxo
líquido, a mais do que isso. O mapeamento de \emph{tags} não reproduz, portanto,
o balanço fora da janela validada. A série avaliada é a do balanço; que as
mudanças de ano para ano que o Capítulo~\ref{cha:diagnostico} encontra não
reflitam também alterações de medição ou de contabilização no próprio
balanço não foi verificado, e fica como limitação
(Secção~\ref{sec:limitacoes}).

\paragraph{O que o modelo não faz, e o que faz.} Não imputa consumo nem
carga: um dia sem valor em nenhum canal fica sem valor. Há, porém, três
preenchimentos declarados. O poder calorífico dos combustíveis lidos da
folha do balanço é propagado do último valor conhecido e, no início de uma
série, preenchido a partir do primeiro valor posterior, com a origem de cada
valor registada. É desta folha que sai o poder calorífico do fuel gás na
série avaliada; o preenchimento para trás usa informação que não existia no
dia, e a contagem desses dias não foi exportada. As propriedades da carga
sem \emph{tag} recebem o valor por omissão do mapeamento (na destilação
atmosférica, só o rendimento de outros produtos, em 73 dos 2\,404 dias). E
um dia sem valor no canal do historiador passa a zero nesse canal. Os dias com
consumo nulo são excluídos da estimação de \emph{baselines}, porque um zero
pode ser físico, uma falta ou um zero fabricado pela fonte e o modelo não
os distingue. Na destilação atmosférica de 2020 a 2025 a exclusão não
altera a população avaliada: os 102 a 138 dias com consumo nulo em cada vetor
diário têm todos carga abaixo do limiar e já estavam fora do domínio
operacional. Noutras unidades isto não foi verificado. Não inclui o gás natural nem o
\emph{tail gas} no perímetro de nenhuma unidade, por decisão de âmbito.
E trata a eletricidade como o que ela é: uma alocação mensal distribuída
uniformemente pelos dias, que o Capítulo~\ref{cha:diagnostico} mostra não
ser uma medição diária.

\section{Cálculo de métricas energéticas}
\label{sec:metricas}

\paragraph{Uma unidade comum.} Todos os vetores são convertidos a
toneladas de gás natural equivalente por dia (t~GNE/d), a unidade em que
a refinaria orçamenta e reporta. Com $m$ a massa diária em toneladas,
\begin{equation}
E_{\mathrm{GNE}} = \frac{m\,\overline{\mathrm{PCI}}}{\mathrm{PCI}_{\mathrm{ref}}}
\;\text{(combustíveis)},\qquad
E_{\mathrm{GNE}} = \frac{m\,h_p}{\mathrm{PCI}_{\mathrm{ref}}}
\;\text{(vapor)},\qquad
E_{\mathrm{GNE}} = \frac{E_{\mathrm{el}}\times 860{,}4}{\mathrm{PCI}_{\mathrm{ref}}\times 0{,}57}
\;\text{(eletricidade)},
\label{eq:conversao-gne}
\end{equation}
em que $\mathrm{PCI}_{\mathrm{ref}} = 11\,820$~kcal/kg é o poder calorífico
inferior de referência do gás natural (norma interna da empresa),
$\overline{\mathrm{PCI}}$ o poder calorífico do dia --- no canal de
\emph{tags}, a média diária da \emph{tag} de poder calorífico; no canal de
balanço, que é o da série avaliada, o valor diário da folha de poderes
caloríficos do balanço de produção ---, $h_p$ a entalpia equivalente do nível de pressão (758, 738 e
698~kcal/kg a 24, 10 e 3~bar, declaradas na tabela de parâmetros como
valores de tabelas de vapor saturado) e $E_{\mathrm{el}}$ a eletricidade em
MWh, com 860,4~Mcal por MWh. Os fatores de mil entre kcal e Mcal e entre kg
e t cancelam-se. As entalpias são equivalentes contabilísticos: a referência
entálpica e o tratamento do retorno de condensado não estão documentados. O
fator 0,57 é uma convenção da empresa que exprime a eletricidade em
combustível equivalente; não é um rendimento medido. O vapor de 10~bar é um
fluxo líquido (consumo menos produção) e pode ser negativo. Como as
constantes são fixas, um erro numa delas escala a série do vetor inteiro e
não muda a forma da relação com a carga; muda o nível e o declive na mesma
proporção, e altera as comparações entre vetores. O t~GNE soma energias de natureza
diferente segundo uma convenção de equivalência e não mede eficiência
termodinâmica. Cada regra está
declarada na tabela de parâmetros por vetor, e o cálculo é feito uma vez,
na ingestão, para que todas as medidas leiam a mesma coluna.

\paragraph{Dia operacional e consumo específico.} Um dia é operacional
para uma unidade quando a sua carga é igual ou superior ao limiar declarado
para essa unidade. O consumo específico é o total de GNE nos dias
operacionais dividido pela carga nesses mesmos dias, em t~GNE por
quilotonelada; as janelas móveis de 7 e 30 dias e o acumulado do período
são calculados sobre a mesma seleção. O limiar não é um parâmetro de
apresentação: é a fronteira do domínio em que a \emph{baseline} é estimada,
e a Secção~\ref{sec:perfil-dados} mostra o que ele exclui.

\paragraph{A \emph{baseline} em produção.} Para cada unidade, vetor e ano
civil, o modelo estima por mínimos quadrados a relação
$E = a\,Q + b$ (o modelo estatístico de \emph{baseline} que a ISO~50006
tipifica~\cite{ISO50006_2014}) entre o consumo diário (em t~GNE/d, na coluna sem
produção) e a carga diária (em kton/d), sobre os dias operacionais do ano
com energia não nula e anteriores ao dia corrente. A estimação é feita
dentro do próprio modelo, numa tabela calculada que materializa, por
célula unidade$\times$vetor$\times$ano, o declive, a ordenada na origem,
o desvio-padrão residual, o coeficiente de determinação e o número de
observações --- 583 células no estado de julho de 2026, incluindo o ano
corrente marcado como parcial. O vetor global de cada unidade é a soma
dos vetores orçamentáveis (o coque, subproduto queimado no regenerador,
mantém reta própria mas fica fora da reta global). Foi esta tabela ---
exportada tal como o modelo a calcula --- que serviu de referência à \ajuste{replicação
bloqueante} da Secção~\ref{sec:replicacao}: o que o
Capítulo~\ref{cha:diagnostico} critica é, com igualdade numérica
verificada, o que este parágrafo descreve.

A reta a aplicar escolhe-se num seletor de ano de referência; o modelo não
muda de referência sozinho em janeiro, e o histórico dos anos de referência
usados pela operação não está documentado. Quando esta dissertação fala de
recalibração anual, refere-se ao desenho --- uma reta por ano civil, estimada
com todos os dias do ano --- e ao uso em que cada ano é lido contra a reta do
ano anterior, que é o que o Capítulo~\ref{cha:diagnostico} avalia. O efeito
de uma recalibração anual sobre um desvio é uma consequência desse desenho,
medida em simulação no Capítulo~\ref{cha:discussao}; não é um episódio
observado nos alarmes da refinaria.

\paragraph{Resíduos, bandas e acumulados.} O resíduo diário é a diferença
entre o consumo observado e o previsto pela \emph{baseline} do ano de
referência selecionado, aplicada à carga do próprio dia; o resíduo
padronizado divide-o pelo desvio-padrão residual dessa \emph{baseline}. As
bandas a $\pm1$, $\pm2$ e $\pm3\sigma$ são as do desenho de produção, e a
regra de alarme é a saída da banda de $\pm2\sigma$. Com $e_t = E_t - \hat E_t$, sobre a série de
resíduos calculam-se o excesso diário $\max(e_t,0)$, o
excesso acumulado $\sum_t \max(e_t,0)$ por vetor, unidade e refinaria, a fração de dias acima
e abaixo da banda, o desvio médio fora da banda, a soma acumulada dos
resíduos desde o início do período selecionado (o gráfico de CUSUM~\cite{Page1954} do
\emph{dashboard}) e o ritmo de excesso nas janelas de 7 e 30 dias. Um
painel de regras de carta de controlo verifica oito regras clássicas de
causas especiais, na linha das de Nelson~\cite{Nelson1984}, sobre os resíduos padronizados --- um ponto fora de $\pm3\sigma$, dois em
três fora de $\pm2\sigma$, quatro em cinco fora de $\pm1\sigma$, oito
consecutivos do mesmo lado, seis monotónicos, catorze alternados, quinze
consecutivos dentro de $\pm1\sigma$, oito consecutivos fora de
$\pm1\sigma$ --- e conta as ocorrências de cada uma. Estas estatísticas
descrevem o método em produção; a sua validade estatística é a pergunta
do Capítulo~\ref{cha:diagnostico}. Duas delas precisam de leitura cuidada.
O excesso acumulado é positivo mesmo com resíduos centrados em zero e cresce
com o ruído e com o número de dias: não estima desperdício nem poupança, e
não compara eficiência entre vetores. O gráfico de CUSUM do
\emph{dashboard} é a soma simples $\sum_t e_t$, um acumulado descritivo; não
é o procedimento de deteção de \textcite{Page1954}, que exige um valor de
referência $k$, um limiar $h$ e uma taxa de falsos alarmes calibrada, e que
é o que o Capítulo~\ref{cha:discussao} avalia com esse nome.

\paragraph{O índice de intensidade energética.} Além da \emph{baseline}
interna, o modelo calcula por unidade o índice de intensidade energética
da metodologia de \emph{benchmarking} da indústria (Solomon): a razão,
em percentagem, entre o consumo real em kBTU e uma energia padrão que
depende da carga em barris e de um factor por unidade --- para a
destilação atmosférica, uma função da densidade da carga, do rendimento em
resíduo e das condições de \emph{flash}, com um teto para cargas leves ---
calculada só nos dias operacionais e com todas as propriedades disponíveis.
O índice é publicado no dia, em janelas móveis de 7 e 30 dias e no
período, contra as bandas de quartil da unidade, e decomposto por vetor
numa cascata. Serve de contexto externo; não é uma \emph{baseline} e não
entra na avaliação do Capítulo~\ref{cha:diagnostico}. A aplicação diária é
um índice adaptado da metodologia, calculado com a versão dos fatores
disponível na refinaria, e não foi validada contra o valor oficial do
estudo de \emph{benchmarking}; não se alega equivalência.

\paragraph{Emissões.} O equivalente em CO$_2$ é calculado do consumo
líquido em GNE com o factor declarado na dimensão dos vetores, no dia e
acumulado, e apresentado ao lado do GNE nas vistas de síntese. O factor é o
mesmo (2,5) para todos os vetores e a unidade não está declarada na tabela.
É um indicador interno de ordem de grandeza: não separa emissões diretas da
combustão das atribuídas ao vapor e à eletricidade, e não é usado nesta
dissertação.
%% CONFIRMAR: unidade e origem do fator_co2 (DimVector, 2,5) — t CO2 por t GNE?

\section{Protocolo de avaliação estatística das \emph{baselines}}
\label{sec:protocolo-baselines}

\paragraph{A lição que definiu o protocolo.} Uma primeira análise das
\emph{baselines}, feita em abril de 2026 fora da plataforma, foi
invalidada em agosto: usava um limiar de carga escrito à mão (15~kton/d)
em vez do limiar oficial lido da configuração (18\,600~t/d), e por isso
analisava uma regressão que não era a que está em produção --- e concluía
coisas que não existem nos dados oficiais. Duas regras saíram daí e não
se negociaram depois: nenhum parâmetro de seleção é escrito à mão, todos
são lidos das tabelas de configuração do modelo de produção; e replica-se
antes de criticar --- se os coeficientes recalculados não reproduzem os de
produção em todas as células, o protocolo para. Uma terceira regra veio da
revisão externa do protocolo: não se criticam violações de independência
usando testes que assumem independência e apresentando os seus
$p$-values como prova.

\paragraph{Pré-registo e revisão adversarial.} O protocolo foi escrito
antes de qualquer execução, no sentido do pré-registo~\cite{Nosek2018} --- perguntas, estimadores, comparadores,
famílias de hipóteses, correção de multiplicidade, limiares e a grelha de
sensibilidade --- e submetido a quatro revisões adversariais independentes
que apontaram vinte defeitos na primeira
versão; a versão 1.2, que se descreve aqui, é a que resultou da
adjudicação desses defeitos. Todos os parâmetros vivem num único ficheiro de
configuração \ajuste{sob controlo de versões}; nenhum é duplicado em
código. As poucas decisões tomadas depois de ver resultados --- sobre desenho,
nunca sobre critérios --- estão datadas no plano de execução e assinaladas
no texto onde entram.

\paragraph{Uma pergunta de dados antes de qualquer análise.} O primeiro
passo não é estatístico: é um perfil dos dados --- cobertura, mecanismo de
falta, distribuições brutas, efeito do filtro de seleção, séries temporais,
relação bivariada, consistência entre fontes --- que uma pessoa lê e
aceita, \ajuste{com declaração assinada e presa à execução que o produziu
por uma assinatura digital do seu conteúdo (\emph{hash}), que muda se um
único byte mudar}, antes de qualquer instrumento arrancar. O erro de abril foi um
erro de seleção de dados que um perfil decente teria exposto; \ajuste{esta
verificação existe para que isso não volte a acontecer, e nenhum programa
pode emitir a declaração por si --- só uma pessoa a pode criar}.

\paragraph{Replicação exata como \ajuste{condição bloqueante}.} Sobre a exportação
congelada do modelo, o protocolo reestima a regressão anual de cada
unidade--vetor com o limiar lido da configuração e compara declive,
ordenada na origem, desvio-padrão residual e coeficiente de determinação
com os da tabela de \emph{baselines} exportada, célula a célula, com
tolerância pré-registada de $10^{-6}$; a identidade dos dias selecionados
é comparada \ajuste{por uma assinatura do conjunto, que muda se um só dia
entrar ou sair}. Trinta células têm de
reproduzir-se; se uma falha, nada a jusante corre. Cada instrumento
posterior repete uma guarda de replicação sobre as células que usa.

\paragraph{O contrato inferencial de cada instrumento.} A
Tabela~\ref{tab:contrato-inferencial} fixa, para cada instrumento, o que
se estima ou se testa, sobre que unidade e que amostra, contra que
comparador, com que intervalo e dentro de que família de multiplicidade
--- e o estatuto que o resultado tem na dissertação. Três fronteiras
importam mais do que as restantes. Os intervalos são \emph{condicionais
nos modelos já ajustados}: o \emph{bootstrap} reamostra a série do ano de
aplicação --- diferenças diárias de perda, ou o indicador diário de
pertença à banda --- com a reta do ano de treino fixa, e nunca reestima o
treino; a incerteza da própria estimação anual entra só pela via dos
erros-padrão HAC nos testes de igualdade, e não nos intervalos de perda ou
de cobertura. Um intervalo de cobertura que contém o valor nominal diz que
os dados não o excluem, não que a banda está calibrada. E a detetabilidade
nos dados reais foi declarada não estimável antes das corridas, porque não
há desvios rotulados; o que existe é a simulação da Parte~C, que descreve o
gerador e não a refinaria.

{\footnotesize
\begin{xltabular}{\linewidth}{@{}>{\raggedright\arraybackslash}p{1.9cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}p{1.8cm}>{\raggedright\arraybackslash}p{2.6cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}p{1.9cm}@{}}
\caption[Contrato inferencial de cada instrumento]{Contrato inferencial
  de cada instrumento do protocolo: o que se estima ou testa, sobre que
  amostra, contra que comparador, com que incerteza e com que estatuto.
  MBB: \emph{bootstrap} de blocos móveis, intervalo percentil a 95\,\%,
  2\,000 réplicas, bloco base $\lceil n^{1/3}\rceil$ dias (7 nas amostras
  anuais). Os cinco pares são 2020${\to}$2021 a 2024${\to}$2025; o par
  2025${\to}$2026 é parcial e só diagnóstico.}\label{tab:contrato-inferencial}\\
\toprule
Instrumento & Estimando ou $H_0$ & Unidade; amostra & Comparador & Incerteza; multiplicidade & Estatuto \\
\midrule
\endfirsthead
\toprule
Instrumento & Estimando ou $H_0$ & Unidade; amostra & Comparador & Incerteza; multiplicidade & Estatuto \\
\midrule
\endhead
\midrule
\multicolumn{6}{r}{\emph{continua na página seguinte}}\\
\endfoot
\bottomrule
\endlastfoot
    Desempenho preditivo (E2) & \revisto{média das diferenças diárias de perda absoluta, nulo $-$ modelo, no ano de aplicação} & dia; 5 pares & média do ano de treino (primário); mesmo dia do ano anterior e média móvel de 30 dias (secundários) & MBB sobre a série de diferenças; sensibilidade a blocos de 7, 14, 30, 60 e 90 dias; por contraste, sem correção & confirmatório; critério C1 de aceitação \\
    \addlinespace
    Estabilidade (E3) & $H_0$: declives anuais iguais na regressão empilhada com interações ano$\times$carga & dia; 6 anos & nenhum (teste de igualdade) & Wald com covariância HAC (\emph{kernel} quadrático-espectral, largura de Andrews); família de quatro hipóteses, uma por vetor, BH; BY como verificação & confirmatório \\
    \addlinespace
    Calibração (E4) & fração de dias do ano de aplicação dentro da banda $\pm2\hat\sigma$ do ano de treino & dia; 5 pares & nominal 0,9545 & MBB sobre o indicador diário; sensibilidade a blocos de 14, 30 e 60 dias; por contraste & confirmatório; critério C2 de aceitação \\
    \addlinespace
    Covariáveis (E6) & $\Delta_{\mathrm{MAE}}$ entre o modelo só com carga e o modelo com $Z$ pré-especificado, no ano seguinte, sem reestimação & dia; 5 pares & \emph{baseline} só com carga & MBB sobre as diferenças diárias; por contraste & confirmatório \\
    \addlinespace
    Detetabilidade (Parte C) & probabilidade de sinal até 90 dias, dada a amplitude e o gerador, ao mesmo orçamento de falsos sinais (5\,\% em 90 dias por canal) & réplica simulada & entre monitores e entre referências & Wilson sobre réplicas; 8 contrastes co-primários sem correção; 440 secundários publicados sem promoção & simulação; nos dados reais, não estimável \\
    \addlinespace
    Critério de aceitação (T13/T14) & decisão C1~$\wedge$~C2, cada um em $\ge 4/5$ pares; sensibilidade a $3/5$ & par & nenhum & herda os intervalos dos instrumentos; não recalcula & adjudicação \\
    \addlinespace
    Diagnósticos (D1--D9) & dependência, variância, caudas, suporte, influência, plausibilidade, correlação & dia & nenhum & sem inferência: $p$-values calculados sob independência e sem interpretação & descritivo \\
    \addlinespace
    Sensibilidade às convenções & reavaliação de cada conclusão em $3 \times 2$ células (limiar $\times$ partição) & célula; 624 reavaliações & célula de referência & rótulo muda ou não muda & qualifica; não cria conclusões \\
    
\end{xltabular}
}

\paragraph{O que o \emph{bootstrap} de blocos pode e não pode.} HAC e MBB
tratam a dependência serial, não a não-estacionariedade: são
assintoticamente válidos para séries estacionárias com dependência fraca, e
a série reamostrada aqui é a das diferenças diárias de perda, ou a dos
indicadores diários de cobertura, no ano de aplicação. Duas coisas os
enfraquecem. A primeira é a persistência: antes de cada corrida, o
protocolo mede a autocorrelação de primeira ordem da própria série
reamostrada, sobre pares de dias consecutivos, e classifica o domínio ---
abaixo de $0{,}90$ o intervalo é reportado como está, entre $0{,}90$ e
$0{,}95$ a sensibilidade aos blocos longos torna-se obrigatória, e acima de
$0{,}95$ o intervalo é despromovido a diagnóstico e não sustenta conclusão. A segunda é a quebra: se a relação
muda a meio do ano de aplicação, \revisto{a média pode variar no tempo, comprometendo a justificação estacionária do intervalo, embora continue definida uma média para o período avaliado;} é o
que a cobertura condicional por semestre (Secção~\ref{sec:calibracao})
existe para apanhar, e é a razão por que um contraste cujo intervalo exclui
o valor de referência por pouco é apresentado como fronteiriço.

\paragraph{Blocos civis com lacunas.} Os blocos são janelas do calendário,
não de posições: um bloco de sete dias é uma janela de sete dias civis
consecutivos, e há uma janela candidata por cada dia de arranque entre o
primeiro e o último dia observado. Cada candidata contém as observações
retidas cujas datas caem na janela; uma janela sem observação nenhuma é
excluída, e uma janela que atravesse uma lacuna --- uma paragem, dias abaixo
do limiar --- fica com menos observações do que o seu comprimento civil, e é
mantida assim, porque é essa a dependência que os dados realmente têm. Cada
réplica sorteia janelas com reposição e concatena as suas observações até
atingir o número de dias da amostra, truncando a última; não há reinício
em fronteira de lacuna, e a dependência entre o fim de uma janela e o início
da seguinte é quebrada, como em qualquer \emph{bootstrap} de blocos. O
\revisto{comprimento base é $\lceil n^{1/3}\rceil$ dias civis, sete ou oito nas amostras anuais avaliadas,} e um comprimento declarado que exceda a extensão civil da amostra é
reportado como inexequível, não substituído. As contagens de janelas
candidatas e a distribuição do seu número de observações acompanham cada
intervalo no Apêndice~\ref{app:tabelas}.

\paragraph{As perguntas e a inferência.} As conclusões do diagnóstico
assentam em cinco perguntas, cada uma com estimador, comparador e
critério declarados: se o modelo de um ano prevê o seguinte melhor do que
referências triviais (média do ano de treino como nulo primário; mesmo dia
do ano anterior e média móvel de trinta dias como secundários~\cite{HyndmanKoehler2006}), medido
pela diferença emparelhada de perda absoluta diária~\cite{DieboldMariano1995,HarveyLeybourneNewbold1997}; se os declives anuais
são iguais, por um teste de Wald sobre uma regressão empilhada com
interações ano$\times$carga, na linha do teste de Chow~\cite{Chow1960}; se a banda de $\pm2\sigma$ do ano de treino
cobre a fração nominal dos dias do ano seguinte; que desvio mínimo o
sistema de alarmes deteta e em quanto tempo; e se covariáveis
pré-especificadas (densidade da carga, temperatura e pressão de
\emph{flash}) acrescentam valor preditivo fora da amostra. A inferência
respeita a dependência serial que os próprios resíduos exibem: erros-padrão
robustos à heterocedasticidade e à autocorrelação~\cite{NeweyWest1987}
(\emph{kernel} quadrático-espectral com largura de banda de
Andrews~\cite{Andrews1991}) para os testes de igualdade, e intervalos percentil~\cite{Efron1979,EfronTibshirani1993} por
\emph{bootstrap} de blocos móveis~\cite{Kunsch1989,Lahiri2003} --- bloco
base de $\lceil n^{1/3}\rceil$ dias~\cite{HallHorowitzJing1995} com
desfasamentos civis exatos, 2\,000 réplicas, semente fixa, sensibilidade a blocos de 7, 14,
30 e 60 dias --- para todos os contrastes de perdas e coberturas. \revisto{Uma conclusão de diferença estatística exige que o intervalo exclua o valor de referência; a compatibilidade usada na aceitação é uma decisão distinta, não uma prova de equivalência.} A multiplicidade é corrigida dentro da
família dos testes de igualdade de declives (Benjamini--Hochberg~\cite{BenjaminiHochberg1995}, com
Benjamini--Yekutieli~\cite{BenjaminiYekutieli2001} como verificação); os restantes resultados são por
contraste e são apresentados como tal. A validação do \emph{bootstrap} por
simulação, a $\rho = 0{,}90$ e bloco de 60 dias, deu uma cobertura de
357/400; o resultado foi adjudicado por teste binomial exato~\cite{ClopperPearson1934}
depois de conhecido e fica declarado como o que é --- ausência de evidência de
cobertura inferior a 0,90, não prova de cobertura nominal. O ensaio é um
diagnóstico e não uma validação: $357/400 = 0{,}8925$, com intervalo exato de
0,86 a 0,92, cobre um único valor de $\rho$ e o bloco de 60 dias, e não se
transfere para o bloco base nem demonstra os 95\% nominais. Por isso, um
contraste cujo intervalo exclui o valor de referência por pouco é
apresentado como fronteiriço no local onde aparece.

\paragraph{Diagnósticos descritivos.} Um segundo conjunto de instrumentos
--- dependência e variância dos resíduos, distribuição e caudas,
segmentação do espaço operacional, sobreposição de suporte entre anos,
influência de semanas, plausibilidade física dos coeficientes, correlação
entre vetores --- documenta mecanismos. Descreve os dados e não faz
inferência: os testes clássicos associados são calculados sob independência,
que os próprios diagnósticos mostram não valer, os seus $p$-values não têm
interpretação inferencial, nenhum rótulo é conclusão, e nenhuma afirmação da
dissertação assenta neles.

\paragraph{Sensibilidade às convenções.} Todas as conclusões são
reavaliadas numa grelha pré-registada que cruza o limiar de carga a 0,8,
1,0 e 1,2 vezes o oficial com duas partições temporais --- anos civis e
janelas de doze meses ancoradas a 1 de julho --- num total de 624
reavaliações, com a célula de referência a reproduzir exatamente os testes
deterministas. A grelha qualifica; nunca cria conclusões.

A Figura~\ref{fig:desenhos-avaliacao} alinha os três desenhos no mesmo eixo temporal e torna visível o que cada um pode e não pode demonstrar.

\input{5-Figures/tikz/f07-linha-temporal}
\paragraph{Demonstração das linhas orientadoras.} A avaliação de
candidatos alternativos (estimadores robustos, bandas com dependência
modelada, critério de aceitação) usa os mesmos cinco pares anuais, com
constantes fixadas a priori ou afinadas apenas no ano de treino, e
conclusões só por intervalo. A formulação original previa uma partição
selada com 2025 intocado; foi substituída, por decisão datada, por
validação temporal encadeada~\cite{Tashman2000,BergmeirHyndmanKoo2018},
porque 2025 já tinha informado o diagnóstico --- e nenhum resultado dessa parte se chama \emph{holdout}. O
critério de aceitação (conteúdo preditivo e banda suportada, cada um em
pelo menos quatro dos cinco pares) foi fixado antes de qualquer resultado.

\paragraph{Referência congelada e simulação.} A exploração da
Secção~\ref{sec:referencia-congelada} congela a relação de 2020, a sua
escala (por validação cruzada em seis blocos contíguos~\cite{BergmeirHyndmanKoo2018})
e o seu estado
inicial, aplica-os a 2021--2025 numa população primária declarada (dias
retidos, dentro do suporte de carga de 2020, com observação e predição
finitas), compara modelos apenas na interseção dessas populações, e mede o
comportamento de \emph{baselines} e monitores com verdade conhecida num
gerador declarado (relação de 2020 com as covariáveis de referência, ruído
autorregressivo, máscara real de dias admissíveis, limiares calibrados sob
a hipótese nula a 5\%, quatrocentas réplicas por célula, oito contrastes
co-primários declarados antes da execução). Todos os artefactos
distinguem, linha a linha, o que foi observado \ajuste{na extração
congelada} do que foi imposto em simulação.

\paragraph{Três formas de avaliar, três estimandos.} A dissertação avalia
\emph{baselines} de três maneiras, que respondem a perguntas diferentes e
não se somam. A primeira, o diagnóstico do Capítulo~\ref{cha:diagnostico},
reajusta o modelo em cada ano e aplica-o ao seguinte: mede a
transportabilidade da relação anual. A segunda, a demonstração das linhas
orientadoras (Secção~\ref{sec:demonstracao}), mantém esses mesmos cinco
pares anuais mas substitui o estimador, a banda ou ambos, e adjudica cada
candidato por um critério declarado a priori: mede se existe um candidato
aceitável dentro de uma classe. A terceira
(Secção~\ref{sec:referencia-congelada}) congela uma referência em 2020 e
aplica-a a 2021--2025 sem lhe tocar: mede o desvio face a um ponto fixo, e o
que cada componente de um sistema de monitorização --- normalizador, escala,
monitor, banda --- faz a esse desvio. A Tabela~\ref{tab:estimandos} fixa os
três antes de qualquer resultado.

\begin{table}[htbp]
  \centering
  \caption[Os três estimandos deste capítulo]{As três formas de avaliar uma
  \emph{baseline} usadas na dissertação, o que cada uma mede e o que não
  pode estabelecer.}
  \label{tab:estimandos}
  \small
  \begin{tabular}{p{3.0cm}p{3.6cm}p{3.6cm}p{3.4cm}}
    \toprule
    Avaliação & Desenho & O que mede & O que não estabelece \\
    \midrule
    Diagnóstico (Cap.~\ref{cha:diagnostico}) & modelo do ano $y$ aplicado a $y{+}1$, cinco pares 2020--2025 & transportabilidade da relação anual; calibração da banda; valor de covariáveis & utilidade para deteção; causa da instabilidade \\
    Demonstração (Secção~\ref{sec:demonstracao}) & os mesmos cinco pares; estimador, banda e critério de aceitação declarados a priori & se algum candidato da classe passa o critério & impossibilidade da classe; necessidade de modelos com estado \\
    Referência congelada (Secção~\ref{sec:referencia-congelada}) & referência de 2020 aplicada a 2021--2025; simulação com verdade conhecida & desvio face a um ponto fixo; o que cada componente faz ao desvio & poupança, alarme validado, mecanismo; factos sobre a refinaria a partir da simulação \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{A classe de candidatos e o critério de aceitação.} A demonstração
da Secção~\ref{sec:demonstracao} avalia uma classe declarada antes de
qualquer resultado. Um candidato é um par (estimador, banda). Os estimadores
são três retas sobre o ano de treino --- mínimos quadrados, que é o método de
produção, Huber e Theil--Sen ---, comparadas pela perda absoluta diária no
ano seguinte, sem reestimação, por contraste emparelhado. As bandas são
quatro: a de produção, $\pm2\hat\sigma$ dos resíduos do treino; duas
construídas sobre um processo autorregressivo ajustado aos resíduos do
treino, com desfasamentos civis exatos e ordem escolhida pelo critério de
informação bayesiano~\cite{Schwarz1978} só no treino, uma gaussiana
($\pm2\hat\sigma$ das inovações) e outra por quantis empíricos das
inovações; e uma por regressão quantílica a $\tau = 0{,}02275$ e
$0{,}97725$, os quantis que correspondem a $\pm2\sigma$ numa normal, com
verificação de não cruzamento que recusa o resultado em vez de o corrigir.
O conjunto avaliado não é o fatorial: as bandas robustas só existem para os
estimadores robustos.

\paragraph{O que o critério aceita, e o que não certifica.} Um candidato é
aceite se cumprir os dois critérios do contrato da
Tabela~\ref{tab:contrato-inferencial} --- conteúdo preditivo sobre o nulo
primário e cobertura compatível com a nominal --- em pelo menos quatro dos
cinco pares anuais, com três em cinco como sensibilidade declarada. A
adjudicação é pura: lê os valores de corridas identificadas e verificadas por
\emph{hash} e não recalcula nada. O que o critério aceita é um par formado
por um preditor da relação com a carga, sob a hipótese de que ela se
transporta para o ano seguinte, \revisto{e uma banda cuja cobertura não se mostrou incompatível com a nominal pelo critério adotado.} A referência congelada lê a mesma perda de cobertura no ano
seguinte como um sinal a preservar, e sem eventos rotulados os dados não
separam as duas leituras: aceitar um candidato não o certificaria como
detetor, e rejeitar todos não retira utilidade a uma referência usada para
medir desvios.

\section{Reprodutibilidade e proveniência}
\label{sec:reprodutibilidade}

A exigência que estrutura toda a infraestrutura é simples de enunciar:
qualquer número citado nesta dissertação tem de poder ser refeito, e uma
alteração posterior a um resultado tem de ser detetável face a um manifesto
identificado~\cite{Peng2011,Sandve2013}. A âncora de confiança é o manifesto
de cada corrida, com o \emph{commit} do código e os \emph{hashes} dos dados.
Integridade não é correção: os \emph{hashes} garantem que o número citado é o
que a corrida produziu, não que o filtro, o estimando ou a interpretação
estejam certos. Os dados industriais não são divulgáveis; um pacote público
com o código, o ambiente e dados sintéticos é trabalho futuro.

\paragraph{Dados congelados e identificados.} As 13 tabelas do modelo
dimensional que a avaliação consome foram exportadas em 2026-07-31 e
copiadas para o repositório de análise como ficheiros de texto só de
leitura; o \emph{hash} SHA-256 de cada um está registado num ficheiro de
fontes, e uma corrida cujo \emph{hash} de entrada não coincida falha em vez
de produzir números diferentes em silêncio. \revisto{A identidade das observações selecionadas pela replicação do método de produção é guardada como \ajuste{assinaturas} de referência; não foi comparada com uma lista independente de dias exportada do Power BI.}

\paragraph{\ajuste{Cada execução fica registada e não se reescreve.}}
\ajuste{Uma corrida é uma execução de um instrumento. Verifica-se antes de
começar --- os dados são os congelados, o ambiente é o declarado, não há
alterações por guardar no código --- e, se passar, publica-se numa pasta
própria que nunca mais é alterada; se não passar, vai para uma pasta de
falhas. Uma corrida substituída não é apagada, deixa apenas de ser citada.
Cada uma leva um manifesto que identifica os dados de entrada, a versão
exata do código e do ambiente, as sementes dos geradores aleatórios e cada
ficheiro produzido. Os instrumentos não aceitam parâmetros científicos na
linha de comandos: tudo o que muda um resultado é lido da configuração, e
não pode ser alterado à mão no momento de correr. A mecânica está no
Apêndice~\ref{app:manual}.}

\paragraph{Pins e verificação independente.} Os resultados que a
dissertação cita são corridas canónicas identificadas (``pinadas'') na
configuração; qualquer instrumento que consuma resultados de outro lê-os
da corrida pinada, verifica o \emph{hash} do manifesto de origem antes de
ler e recusa o lote errado. Um verificador que não importa nada do
repositório --- só as bibliotecas de \emph{hash} e de leitura de texto ---
recomputa, para cada corrida citada, os \emph{hashes} e tamanhos de todos
os ficheiros e o \emph{hash} do inventário. À data desta versão, 110
manifestos e 1\,481 ficheiros foram verificados sem divergência por uma
auditoria independente.

\paragraph{Números nos documentos.} Os relatórios de resultados não
contêm números escritos à mão: cada valor da exploração com referência
congelada é derivado das tabelas pinadas por um gerador de documentos e
registado num bloco de proveniência no fim de cada documento, que um teste
automático recalcula e confronta com a prosa. As tabelas dos capítulos de
resultados desta dissertação seguem a mesma origem; a Secção~\ref{sec:limitacoes}
diz onde a garantia é mais fraca do que o mecanismo sugere.

\paragraph{Testes e ambiente.} \ajuste{O código de avaliação tem mais de
mil e setecentos testes automáticos, repartidos pelos dois ambientes; sem
teste, um instrumento não está feito.} \ajuste{A lição que ficou é outra: um
teste que verifica a presença de um campo não verifica o significado do
filtro que o produziu --- registada depois de uma revisão ter apanhado cinco
afirmações que a tabela por trás delas refutava.}
O determinismo é declarado com precisão: as corridas são reprodutíveis no
ambiente fixado com igualdade numérica até cerca de $10^{-15}$; a igualdade
byte a byte de ficheiros com valores em vírgula flutuante depende da
biblioteca de álgebra linear da máquina, e não é prometida.

\paragraph{Trilha adversarial.} As revisões externas do protocolo, as
auditorias científicas de cada parte (a integridade da cadeia de
resultados foi aprovada e a conformidade científica reprovada em setembro
de 2026, com correções feitas no código e testes que reproduzem cada
achado, e uma segunda reinspeção ainda em curso à data desta versão) e as
decisões do autor que delas resultaram estão versionadas no repositório.
É a parte da metodologia que
mais custou e a que mais vale: cinco afirmações da primeira versão dos
resultados eram falsas, todas do mesmo tipo --- narrativa à frente da
verificação ---, e nenhuma sobreviveu a abrir a linha da tabela que a
devia sustentar.
